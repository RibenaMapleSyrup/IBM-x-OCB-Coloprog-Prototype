import { ScatterChart } from "./scatter-chart";
import { ChartConfig, LineChartOptions } from "./configuration";
export declare class LineChart extends ScatterChart {
    lineGenerator: any;
    options: LineChartOptions;
    constructor(holder: Element, configs: ChartConfig<LineChartOptions>);
    draw(): void;
    interpolateValues(newData: any): void;
    updateElements(animate: boolean, gLines?: any): void;
}
